# 开发规范

## 1. 功能模块管理
- 每次新增功能后都应该在GUI中添加对应的功能模块入口
- 确保新功能在主窗口的选项卡中可见
- 为新功能添加适当的图标和文字描述

## 2. 弹窗管理
- 所有的二级弹窗都应该居中显示在此软件的中间位置
- 弹窗大小应该适中，确保所有内容都能完整展示
- 弹窗应该设置为模态窗口，防止用户操作其他部分

## 3. 测试规范
- 每次新增功能都要做自动化测试，确保没有bug
- 测试文件都放在src/tests文件夹内
- 测试应该覆盖功能的主要场景和边界情况
- 测试通过后才能提交代码

## 4. 文档规范
- 每次测试完功能后没有问题都应该补充对应的文档说明到项目根目录的README.md中
- 文档应该详细描述功能的使用方法、参数说明和注意事项
- 保持文档与代码同步，确保文档的准确性

## 5. 日志管理
- 所有功能操作日志都应该使用src/utils/logger.py来处理
- 日志应该分级（info, warning, error）
- 日志内容应该清晰、准确，包含必要的上下文信息

## 6. 性能优化
- 代码都应该考虑多线程处理和性能优化
- 对于大量文件的处理，应该使用多线程或异步处理
- 避免不必要的计算和IO操作

## 7. 错误处理
- 事件判断都需要做兜底处理，避免不必要的BUG
- 使用try-except捕获异常，并提供合理的错误提示
- 对用户输入进行验证，防止无效输入导致的错误

## 8. 代码兼容性
- 不要无故删除之前已经实现的功能和GUI，除非用户有要求
- 修改现有功能时，确保向后兼容
- 保持API的稳定性，避免破坏现有代码

## 9. 代码组织
- 公共方法和类应该提取出来放在src/utils文件夹下
- 代码应该按照功能模块进行组织，保持目录结构清晰
- 遵循项目的代码风格和命名规范

## 10. 代码审查
- 所有提交的代码都应该审查，以避免不必要的bug
- 审查重点包括：功能正确性、代码质量、性能、安全性
- 确保代码符合项目的开发规范

## 11. 版本控制
- 每次修改代码前应该先拉取GitHub上的代码，防止代码冲突
- 提交代码时应该写清楚提交信息，描述修改的内容和原因
- 定期合并代码，保持分支的同步

## 12. 分支管理
- 代码提交需要同步到dev分支和main分支上
- 默认使用dev分支作为开发分支
- main分支为bate分支，用于发布稳定版本
- 重大功能开发应该创建单独的特性分支