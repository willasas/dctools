# 详细文件分析功能说明

## 功能概述

新增的"详细文件分析"功能可以获取指定文件夹内所有文件的详细信息，包括：

- **文件名称** - 文件的完整名称
- **文件扩展名** - 文件的类型后缀（如 .txt、.jpg、.docx 等）
- **文件大小** - 文件的大小（字节数和格式化显示）
- **文件完整路径** - 文件在系统中的完整路径
- **文件修改时间** - 文件最后修改的时间戳
- **文件创建时间** - 文件创建的时间戳

## 使用方法

### 1. 启动程序
双击运行 `run.bat` 文件启动程序

### 2. 选择文件夹
通过以下方式选择要分析的文件夹：
- **批量添加文件夹**按钮：打开多文件夹选择窗口
- **拖放文件夹**：直接将文件夹拖放到程序界面
- **详细文件分析**按钮：点击开始详细分析

### 3. 选择输出格式
程序会弹出格式选择对话框：
- **TXT 文本格式**：生成易于阅读的文本报告
- **CSV 表格格式**：生成适合Excel等软件处理的表格数据

### 4. 查看分析结果
分析完成后，程序会生成包含时间戳的结果文件：
- `detailed_files_analysis_YYYYMMDD_HHMMSS.txt`
- `detailed_files_analysis_YYYYMMDD_HHMMSS.csv`

## 输出格式说明

### TXT格式输出
- 按文件夹分组显示
- 每个文件显示详细信息
- 适合直接阅读和打印

### CSV格式输出
- 标准的表格格式
- 适合导入Excel、数据库等
- 包含所有字段的完整数据

## 技术特点

1. **全面信息收集**：获取文件的完整元数据信息
2. **智能格式化**：文件大小自动转换为合适的单位（B/KB/MB/GB）
3. **错误处理**：遇到无法访问的文件时会跳过并继续处理
4. **进度显示**：实时显示分析进度和状态
5. **时间戳命名**：避免文件覆盖，便于版本管理

## 使用场景

- **文件管理**：快速了解文件夹内容结构
- **数据备份**：生成文件清单用于备份验证
- **空间分析**：分析磁盘空间使用情况
- **项目文档**：生成项目文件结构文档
- **数据迁移**：准备文件迁移清单

## 注意事项

- 程序需要读取文件夹的访问权限
- 对于大型文件夹，分析可能需要一些时间
- 结果文件保存在程序运行目录下
- 建议定期清理不再需要的结果文件